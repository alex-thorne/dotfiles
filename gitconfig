# This is Git's per-user configuration file.
[user]
name = {{  git.name }}
email = {{ git.email }}
{% if git.signingkey %}
signingkey = {{ git.signingkey }}
[commit]
gpgsign = true
{% endif %}
[status]
submoduleSummary = True
[init]
defaultBranch = main
